Task 40 release execution preflight

Date: 2026-05-29

Completed:
- Latest main commit: fe8e18f fix(examples): use release distribution for plugin dependency [T-40-prep]
- GitHub Actions after fix:
  - CI 26611613576: success
  - Docs 26611613582: success
  - Licenses 26611613627: success
- Local release pre-flight:
  - `bash scripts/release.sh --pre-flight-check` -> `pre-flight ok for 0.0.0`
- Release dry-run:
  - `bash scripts/release.sh --dry-run --bump=minor` plans 0.0.0 -> 0.1.0

Blocked:
- `scripts/release.sh --bump=minor` creates a signed tag with `git tag -s`.
  Local checks found no configured GPG signing key:
  - `gpg --list-secret-keys --keyid-format=long` -> no secret keys
  - `git config --get user.signingkey` -> empty
- `gh secret list --repo smilebank7/physlab` returned no secrets. The release
  workflow publishes npm with `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`.
- `gh api repos/smilebank7/physlab/environments` returned no configured
  publish environments, while the plan requires explicit human approval for
  release publishing.

Decision:
Do not run the real public release yet. T40 remains unchecked until signing,
publish credentials/environments, GitHub Release, PyPI install, npm install,
and fresh-install anchor smoke verification all succeed.
