# TypeScript MCP API

The TypeScript MCP reference is generated with TypeDoc from the MCP server
sources:

```bash
cd mcp-server
bun run docs
```

The generated HTML is written to `docs/_build/ts/` during local and CI docs
builds.
