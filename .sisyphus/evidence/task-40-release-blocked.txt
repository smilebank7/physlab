Task 40 release execution preflight

Date: 2026-05-29

Completed:
- Latest main commit before this check: 8a107b5 chore(release): use npm trusted publishing [T-40-prep]
- GitHub Actions after fix:
  - CI 26611613576: success
  - Docs 26611613582: success
  - Licenses 26611613627: success
- Local release pre-flight:
  - `bash scripts/release.sh --pre-flight-check` -> `pre-flight ok for 0.0.0`
- Release dry-run:
  - `bash scripts/release.sh --dry-run --bump=minor` plans 0.0.0 -> 0.1.0
- Release workflow hardening:
  - npm publish no longer uses `NODE_AUTH_TOKEN`; it uses GitHub Actions OIDC
    trusted publishing with Node 24.
  - Added `tests/integration/test_release_workflow.py` to reject reintroducing
    long-lived npm tokens or explicit `--provenance`.
- Release readiness hardening:
  - Added `tools/check_release_readiness.py` and unit coverage for GPG signing,
    trusted publishing workflow checks, and GitHub publish environments.
  - `uv run python tools/check_release_readiness.py --repo smilebank7/physlab`
    currently fails only on external release prerequisites:
    - GPG signing: `git user.signingkey` is not configured.
    - GitHub environments: `npm` and `pypi` are missing.
- Verification after hardening:
  - `uv run ruff check .` -> pass
  - `uv run mypy src tests tools` -> pass
  - `uv run pytest -q` -> 165 passed, 2 skipped
  - `PATH="$PWD/.venv/bin:$PWD/mcp-server/node_modules/.bin:$PATH" python tools/check_licenses.py` -> pass
  - `bash scripts/release.sh --dry-run --bump=minor` -> pass
  - `uv run pytest tests/unit/test_release_readiness.py tests/integration/test_release_workflow.py -q` -> 7 passed

Blocked:
- `scripts/release.sh --bump=minor` creates a signed tag with `git tag -s`.
  Local checks found no configured GPG signing key:
  - `gpg --list-secret-keys --keyid-format=long` -> no secret keys
  - `git config --get user.signingkey` -> empty
- The npm workflow no longer requires a long-lived `NPM_TOKEN`; it uses GitHub
  Actions OIDC trusted publishing with Node 24. `tests/integration/test_release_workflow.py`
  guards this.
- The PyPI and npm trusted publisher records still must exist on the package
  registry side before first public publish.
- `gh api repos/smilebank7/physlab/environments` returned no configured publish
  environments, while the plan requires explicit human approval for release
  publishing.

Decision:
Do not run the real public release yet. T40 remains unchecked until signing,
trusted publisher records/environments, GitHub Release, PyPI install, npm
install, and fresh-install anchor smoke verification all succeed.
